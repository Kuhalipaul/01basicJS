// PEIMITIVE datatype[copy karke usmai changes hota hai]

//  -7category: String, Number, Boolean, null, undefind, symbol, BigInt
//********************************************************************/
// JavaScript is dynamically typed. 
// This means that the type of a variable is determined at runtime, not at compile time.
const scroe=100;
const scoreValue=100.3


const isLoggedIn=false
const outsideTemp=null
let userEmail;

const id=symbol("123")
const anotherId= Symbol('123')

// console.log(id=== anotherId);

const bigNumber=3547893458n

//********************************************************************************/
//Reference type or (Non Primitive type)

//Array, Objects, Functions

const heros= ["shaktiman", "naagraj", "doga"];
let myObj={
    name:"kuhali",
    age:25,
}

const myFunction=function(){
console.log("hello world");
}

console.log(typeof bigNumber);